Replace the current damping/friction with a two-phase deceleration model (sliding vs rolling) for more realistic billiards feel.
Requirements:

In PhysicsConstants, add:

double SLIDING_DECEL_PX_PER_S2 (e.g., 900)

double ROLLING_DECEL_PX_PER_S2 (e.g., 220)

double ROLLING_THRESHOLD_PX_PER_S (e.g., 250)

keep STOP_EPS_PX_PER_S (e.g., 8)

In PhysicsWorld.step(dt) after integrating positions, apply friction like:

compute speed s = vel.length()

if s <= STOP_EPS => vel = Vector2.ZERO

else choose decel = (s > ROLLING_THRESHOLD ? SLIDING_DECEL : ROLLING_DECEL)

reduce speed: newS = max(0, s - decel * dt)

set vel = vel.normalized() * newS (avoid divide-by-zero)

Keep collision response as-is; friction should act every tick.

Update debug overlay to show which mode is active for the cue ball: “SLIDE” or “ROLL” and current speed.

Update/relax tests:

speed decreases after step

eventually stops after enough steps
(avoid asserting exact numeric values)

Ensure .\mvnw.cmd test passes and javafx:run feels more realistic.