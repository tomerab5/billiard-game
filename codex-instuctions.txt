Implement cue–ball strike with tip offset and spin generation.
Requirements:

Replace “set velocity on release” with a CueStrike model:

On mouse release, compute cue direction in table plane.

Add an aiming UI for tip offset:

While aiming, show a small marker on the cue ball indicating hit point (dx, dy) relative to center in ball-radius units.

Use mouse wheel or right-drag to adjust offset, clamp to a safe max (e.g., 0.6R) to avoid impossible hits.

Compute an impulse J along cue direction based on charge (existing).

Convert tip offset into angular impulse:

Apply linear impulse at an offset point on the ball surface so w changes via Δw = I^{-1}(r×J).

Add a simple miscue limit:

Cap tangential component based on mu_tip * Jn (use a configurable MU_TIP).

Add tests:

Top hit -> positive roll (w makes ball “follow”).

Bottom hit -> draw behavior (w opposite direction).

Add keys:

C centers tip.

X quick presets: center/top/bottom/left/right.