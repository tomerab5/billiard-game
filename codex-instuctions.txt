codex @'
POCKET TUNING: side-entry pushed away + hanger balls don't fall

Symptoms:
1) Balls entering from the side get pushed back out (facings repel too much).
2) Ball can sit partially in pocket mouth and never "falls" in (needs hanger capture / funnel).

Goals:
- Facings: near-miss bounces OUT, but true entries are NOT repelled.
- Hanger: if ball is sufficiently over the drop and slow, it transitions to ENTERING_POCKET and is pulled/damped in.

Do:
A) Fix facing collision response to avoid "push away"
1) Ensure facing normals are correct and stable:
   - For each facing segment, compute n = perp(normalize(b-a)).
   - Choose sign so n points toward table interior:
     - pick a test point = closestPoint + n * (ballR*0.25)
     - if test point is NOT inside bed rect -> flip n
2) One-sided resolve:
   - only if signedDistance < 0 AND dot(v, n) < 0 (approaching)
3) Set facing material to be non-trampoline:
   - Add PhysicsConfig.POCKET_FACING_RESTITUTION (default 0.08)
   - Add PhysicsConfig.POCKET_FACING_FRICTION (default 0.35–0.6)
   - Use these for facing collisions (not rail restitution)

B) Add hanger capture + funnel (2D approximation of gravity/slope)
1) Add config:
   - POCKET_HANGER_SPEED_M_PER_S (e.g. 0.15)
   - POCKET_HANGER_CAPTURE_DEPTH (fraction of ballR, e.g. 0.35)
   - POCKET_FUNNEL_ACCEL_M_PER_S2 (e.g. 2.5)
2) For ON_TABLE balls each step:
   - find nearest pocket center + mouthRadius
   - if distToCenter <= mouthRadius - ballR*POCKET_HANGER_CAPTURE_DEPTH
     AND |v| <= POCKET_HANGER_SPEED_M_PER_S:
        transition to ENTERING_POCKET and store that pocket center for the ball
3) In ENTERING_POCKET update:
   - apply strong damping (existing approach damping ok)
   - add inward acceleration: v += dirToCenter * POCKET_FUNNEL_ACCEL_M_PER_S2 * dt
   - clamp max speed while entering (so it doesn't slingshot)
   - once distToCenter <= (mouthRadius - ballR*0.75) OR timeEntering > 0.6s:
       set IN_POCKET/REMOVED (existing behavior)

C) Debug so we can see what's happening:
- Overlay line showing for nearest pocket:
  distToCenter, mouthRadius, hangerEligible(true/false), lastFacingHitPocketId

Commands to locate/edit:
- rg -n "facing|facings|one[- ]sided|signedDistance|Pocket|ENTERING_POCKET|IN_POCKET|no-escape|capture" src/main/java/com/billiardgame/physics/PhysicsWorld.java
- rg -n "overlay|debug|P\\b|O\\b|RAILS" src/main/java/com/billiardgame/ui/BilliardApp.java

Run: .\\mvnw.cmd test
Output: changed files + new PhysicsConfig constants + the hanger condition.
'@
