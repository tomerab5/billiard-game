Upgrade 2.5D visuals for realism (Canvas only), keeping physics unchanged.
Requirements:
Table rendering

Add a subtle cloth texture: procedurally generate a small noise image (e.g., 256x256) once at startup and tile it over the cloth area with low opacity.

Add cloth lighting gradient: slightly brighter near the light direction, darker near pockets/rails.

Make pockets visually deep: dark radial gradient inside each pocket.

Ball rendering improvements

Keep existing shaded sphere + specular highlight.

Improve contact shadow:

soft elliptical shadow under ball

offset opposite light direction

shadow size increases slightly with speed (tiny effect) and decreases when ball is near a rail (occlusion cue)

Add a small “brand dot” decal on the cue ball (and optional tiny dot on object balls) that rotates using the physics quaternion markers logic (so spin is visible but subtle and realistic).

Debug toggles

H toggles table texture/lighting

V toggles decals

Keep performance good (cache generated textures). Ensure mvn test passes.