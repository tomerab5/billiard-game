CUE BALL SCRATCH BUG: when cue ball is potted, cue appears but player cannot shoot.

Acceptance (manual):
- If cue ball enters a pocket, it is treated as SCRATCH.
- After all balls come to rest, cue ball respawns on-table and player can aim and shoot normally.
- No "ghost aiming" using an inactive cue ball.

Implement minimal rules (no full 8-ball rules yet):
- Add a flag scratchPending.
- On cue ball pocket capture: set scratchPending=true; set cue ball state REMOVED/IN_POCKET as appropriate.
- When world is at rest AND scratchPending:
  - respawn cue ball at a safe default (e.g., table center or head spot) with v=0, omega=0, pocketZ=0, state ON_TABLE, active=true
  - clear scratchPending
  - set UI state to AIMING and allow shot.

Also ensure:
- Any code path that builds aim guide / cue position checks cueBall.active && cueBall.state==ON_TABLE.

Do:
1) Locate where pocket capture happens and where ball states are set.
2) Locate UI input handling for shooting (mouse release) and the AIMING/CHARGING state machine.
3) Add scratch flow described above.
4) Run: .\mvnw.cmd test

Output:
- Changed files
- Where scratchPending is set/cleared
- Where cue ball is respawned and UI state reset
'@