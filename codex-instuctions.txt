codex @'
POCKET BLOCKER FIX: side pockets still not enterable. Temporarily remove/disable yellow pocket mouth colliders (jaw/facing) for PHYSICS collisions, and ensure ONLY segmented rail segments are used for bed containment.

Acceptance (manual):
- Slow roll into top-middle and bottom-middle pockets enters (no bounce-back).
- Corner pockets remain enterable too.
- There is no global rectangular bounds reflection anywhere.
- P debug can still DRAW pocket jaw/facing shapes, but they DO NOT collide while we’re validating mouth openings.

Do:
1) Find all table-boundary enforcement:
   rg -n "minX|maxX|minY|maxY|bounds|clamp|contain|outOfBounds|resolve.*rail|resolve.*cushion|segments" src/main/java/com/billiardgame/physics/PhysicsWorld.java

2) Ensure there is exactly ONE collision path for rails:
   - collisions must use the segmented rail list that includes gaps at mouths.
   - delete/disable any other bounds clamp / full-length rails.

3) Disable pocket mouth colliders for physics (temporary validation step):
   - In PhysicsConfig add a boolean like POCKET_MOUTH_COLLIDERS_ENABLED default false (or a debug toggle).
   - When false, skip resolving collisions against pocket facingSegments and jawArcs entirely.
   - Keep shelf/capture logic unchanged.

4) Improve debug visualization so we can verify the real blocker:
   - P overlay draws ALL rail segments and their endpoints (dots).
   - Ensure side-pocket gaps are visible as missing rail segments.

5) Run: .\mvnw.cmd test

Output:
- Changed files
- Confirmation of what bounds logic was removed (if any)
- Where the POCKET_MOUTH_COLLIDERS_ENABLED gate is applied
- Rail segment count and proof gaps exist at side pockets
'@
