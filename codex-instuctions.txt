AIM GUIDE FIX: The 2nd yellow segment now stops correctly, but when that 2nd segment hits a BALL (after a rail bounce), we must ALSO draw the WHITE line predicting the struck ball’s trajectory — same behavior as when the 1st segment hits a ball.

Acceptance:
- If segment1 hits ball -> draw white object-ball line (existing behavior).
- If segment1 hits rail and segment2 hits ball -> draw white object-ball line at segment2 end (NEW requirement).
- White line direction should be computed from the predicted collision geometry at that segment’s impact:
  Use cueCenterAtImpact = segOrigin + dir * tHit (tHit returned by raycast vs expanded radius Rsum = cueR + targetR),
  then normal = normalize(targetCenter - cueCenterAtImpact),
  then draw the white line from targetCenter along 'normal'. No extra yellow segments.

Do:
1) Locate aim guide code:
   rg -n "drawAimGuide|trajectory|predict|raycast|HitType|white" src/main/java/com/billiardgame/ui/BilliardApp.java
2) Ensure the segment2 hit branch (HitType.BALL) calls the SAME helper used for segment1 to draw the object-ball white line.
   If there is no shared helper, create one: drawObjectBallPrediction(Ball target, Vec2 cueCenterAtImpact, Vec2 dirIncoming)
   and call it from BOTH segment1-ball-hit and segment2-ball-hit.
3) Make sure you compute cueCenterAtImpact correctly for segment2 (even if you use an epsilon start offset, keep it consistent with the tHit used).
4) Run: .\mvnw.cmd test
5) Print changed files + brief explanation.

Goal: end of 2nd yellow line on ball hit always spawns the white object-ball line.
'@