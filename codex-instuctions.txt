Implement realistic pocket mouths (jaws) and pocket acceptance.
Requirements:

Define pocket geometry:

6 pockets with mouth opening segments + rounded jaw arcs (2D).

Model them as collision primitives (line segments + arcs) that interact like rails (normal/tangent impulses).

“Shelf”/drop rule:

Once the ball center crosses a defined “drop line” inside the pocket, mark it potted and remove from world.

Add tests:

Straight-in slow shot pots reliably.

A fast shot near jaw can rattle and not always pot (deterministic test: choose a specific setup that should reject).

Add a debug draw toggle showing pocket primitives.