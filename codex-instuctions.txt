codex @'
TEST: Are yellow pocket mouth colliders blocking side pockets?

1) Add PhysicsConfig flag: POCKET_MOUTH_COLLIDERS_ENABLED (default false).
2) In PhysicsWorld pocket collision resolution, guard ALL jaw/facing collision impulses with that flag:
   - if (!POCKET_MOUTH_COLLIDERS_ENABLED) skip collisions against jawArcs + facingSegments
   - keep rails + shelf/capture logic unchanged
3) Keep drawing yellow shapes in P overlay (visual only).
4) Run .\mvnw.cmd test

Goal: With flag=false, balls should enter side pockets if yellow colliders were the blocker.
'@
