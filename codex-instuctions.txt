Implement cushions + mouse hold-to-shoot.
Requirements:
Cushions / table borders

Add a TableBounds (or similar) in physics: left, right, top, bottom in world coordinates.

In PhysicsWorld.step(dt) after integration, resolve ball-vs-rect collisions (rails): clamp position and reflect velocity component, multiplying by RAIL_RESTITUTION constant in PhysicsConstants.

Ensure balls remain inside bounds and bounce naturally.

Mouse hold-to-shoot

Remove the “press 1” shot.

In BilliardApp, implement:

On primary mouse press: start charging if all balls are nearly stopped (speed < stop epsilon). Record press timestamp.

While held: compute chargeSeconds and show a power indicator (simple bar or text).

On release: compute shot direction from cue ball to mouse cursor, normalize it, compute speed = min(MAX_SHOT_SPEED, (chargeSeconds / CHARGE_TIME_TO_MAX) * MAX_SHOT_SPEED). Set cue ball velocity = direction * speed.

Add constants in PhysicsConstants or a new InputConstants: MAX_SHOT_SPEED, CHARGE_TIME_TO_MAX.

Keep physics classes free of JavaFX types.

Tests

Add/extend tests for rails:

ball moving right hits right wall and vx becomes negative

ball stays within bounds after step

Ensure .\mvnw.cmd test passes and javafx:run lets me shoot by holding left click.