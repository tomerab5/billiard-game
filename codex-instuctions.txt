Upgrade rail collisions to impulse-based response including tangential friction and spin transfer.
Requirements:

Replace simple “flip vx/vy” rail handling with a contact impulse model:

Use wall normal n and tangent t (both in table plane).

Compute contact point relative vector r = -R*k (bottom contact) OR r = -R*n (if you modeled wall contact differently). Choose one consistent approach and document it.

Compute contact relative velocity vContact = v + w×r.

Compute normal impulse to satisfy restitution e_rail.

Compute tangential impulse along t to oppose slip, clamped by |Jt| <= mu_rail * Jn.

Apply to v and w using the same impulse formulas as ball–ball.

Add tests:

Ball hits vertical rail: vx reverses sign and magnitude reduced by restitution.

With side spin (wz), after rail hit the outgoing vy differs from no-spin case (assert difference).

Add debug overlay values: show mu_rail, e_rail.