codex @'
BUG: Balls that reach a pocket keep going and leave the table (escape the world).

Goal: ON_TABLE balls must NEVER escape the simulation.
If a ball exits bed bounds near a pocket mouth, treat it as entering the pocket (ENTERING_POCKET) and apply damping/funnel. If it exits elsewhere, clamp/reflect (should not happen).

Acceptance (manual):
- No ball can travel outside the table forever.
- A ball that passes through a pocket opening transitions to ENTERING_POCKET and gets damped inward (eventually captured/removed).
- Side/corner near-misses still bounce once we add facings later, but for now: no escape.

Do:
1) Find bed bounds and pocket centers/radii used by PocketModel:
   rg -n "bedMin|bedMax|table.*Min|table.*Max|PocketModel|pocketCenters|corner|side.*pocket" src/main/java/com/billiardgame/physics/PhysicsWorld.java

2) Implement helper:
   - PocketHit classifyPocketExit(Vector2 p, double ballR)
     returns nearest pocket center + mouthRadius + type if p is within (mouthRadius + ballR*1.5)

3) In PhysicsWorld.step(...) AFTER integration + normal collisions, for each ball with state ON_TABLE:
   - if outside bed bounds by eps (e.g. ballR*0.25):
       - PocketHit hit = classifyPocketExit(p, ballR)
       - if hit != null:
           set state=ENTERING_POCKET
           store target pocket center for that ball
           apply immediate damping: v*=0.5, omega*=0.5 (or config)
       - else:
           clamp position back inside bed and reflect velocity (debug safety)

4) In ENTERING_POCKET update:
   - apply strong damping each step
   - apply small acceleration toward pocket center (funnel) so it can’t drift away
   - after a short time or when within capture radius, transition to IN_POCKET/REMOVED (existing logic)

5) Add debug counter on overlay (P or O):
   - pocketEscapesCaught++
   - pocketEscapesClamped++
   so we can confirm the guard triggers.

6) Run: .\mvnw.cmd test

Output:
- Changed files
- The exact outside-bounds condition
- The pocket classification radius used
- Debug counters location
'@
